package main.com.grigorev.cube.enums;

/**
 * Edges of the cube.
 * Also represents slots on cube
 */
public enum EdgePosition {
    UR, UF, UL, UB, // Up layer (U)
    DR, DF, DL, DB, // Down layer (D)
    FR, FL, BL, BR, // Middle layer (E)
}

