package main.com.grigorev.cube.enums;

/**
 * Corners of the cube.
 * Also represents slots on cube
 */
public enum CornerPosition {
    URF, UFL, ULB, UBR, // Up layer (U)
    DFR, DLF, DBL, DRB, // Down layer (D)
}
